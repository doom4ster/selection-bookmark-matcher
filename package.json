{
  "name": "selection-bookmark-matcher",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:mv3": "node scripts/build.js mv3",
    "build:mv2": "node scripts/build.js mv2",
    "build": "node scripts/build.js mv3",
    "release:mv2": "npm run build:mv2 && cd dist/mv2 && zip -r ../../selection-bookmark-matcher-mv2.zip .",
    "release:mv3": "npm run build:mv3 && cd dist/mv3 && zip -r ../../selection-bookmark-matcher-mv3.zip .",
    "release": "npm run release:mv2 && npm run release:mv3"
  }
}
